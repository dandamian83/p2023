// use learning::{greet, exercise_c_simple_types};
use learning::{exercise_e_ownership_and_references};

fn main() {
    // greet();
    // exercise_c_simple_types();
    exercise_e_ownership_and_references();
}
